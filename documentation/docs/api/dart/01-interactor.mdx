---
title: Interactor
---

# API

### Interactor

Use `Interactor` when you need to register a [worker](worker). 

:::tip
  You could call the `shutdown` method if you want to stop all workers.
:::

```dart title="Declaration"
class Interactor {
  Interactor({String? libraryPath})
  Future<void> shutdown() async
  SendPort worker(InteractorConfiguration configuration)
}
```

```dart title="Example"
final interactor = Interactor();
final worker = InteractorWorker(interactor.worker(InteractorDefaults.worker()));
/* Do some work */
await interactor.shutdown();
```

# References
* See [`InteractorConfiguration`](configuration#InteractorConfiguration)
* See [`InteractorDefaults`](configuration#InteractorDefaults)