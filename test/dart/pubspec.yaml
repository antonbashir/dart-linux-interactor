name: linux_interactor_test
version: 1.0.0
publish_to: none

environment:
  sdk: ">=3.0.0 <4.0.0"

dev_dependencies:
  ffigen: ^9.0.1

dependencies:
  ffi: ^2.1.0
  test: ^1.22.0
  linux_interactor:
    path: ../../dart/

ffigen:
  name: "TestBindings"
  description: "Bindings for Test"
  output: "bindings.dart"
  headers:
    entry-points:
      - "../native/test.h"
